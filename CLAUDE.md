# 유튜브 채널 운영 자동화 스위트 - Claude Code 지침

> 프로젝트 초기화 완료 (2026-04-27)
> 세부 정보: PROJECT.md / 작업 현황: PROGRESS.md

---

## [프로젝트] 개요

유튜브 채널 운영 자동화 도구 스위트입니다.
- 데스크톱 앱(CustomTkinter) + FastAPI 백엔드 구조
- 개인 사용: 로컬 실행 / 팀 공유: 서버 배포 전환 가능
- PyInstaller로 .exe 단일 파일 배포

---

## [철학] 핵심 운영 원칙

### 원칙 1: 일 쪼개기 (Task Decomposition)
- 모든 업무를 단계별로 분해하여 수행합니다
- 업무를 항상 여러 단계로 분해하고, PROGRESS.md에 기록
- 각 단계는 독립적이고 검증 가능한 단위
- **각 단계 완료 후 사용자 승인** 받고 다음 단계 진행
- 작업 트리 구조로 순차/병렬 실행 관리

### 원칙 2: 문서화 필수 (Documentation First)
- 어떤 작업이 완료되면 **반드시** 관련 문서를 최신화
- PROGRESS.md: 전체 진행 현황 업데이트
- 해당 모듈의 README.md: 변경사항 반영
- 문서와 실제 코드/상태는 항상 일치해야 합니다

### 원칙 3: 모듈화 (Modularization)
- 기능 단위로 독립적 모듈화
- 각 모듈은 자체 폴더, README, 테스트 보유
- 모듈 간 직접 의존성 최소화

### 원칙 4: 문서 항상 최신화 (Living Documentation)
- 코드 변경 시 문서 동시 업데이트 (같은 커밋)
- PROGRESS.md에 현재 위치, 다음 할 일이 항상 명시
- 누가 이어받더라도 현황을 즉시 파악 가능

### 원칙 5: 엄격한 네이밍 규칙
- **작업 현황 문서**: `PROGRESS.md` (프로젝트 루트)
- **프로젝트 개요**: `PROJECT.md` (프로젝트 루트)
- **리서치 결과**: `research/[NN]-[topic-name]/[subtopic].md`
- **모듈 문서**: `[module-path]/README.md`
- **설계 결정**: `docs/decisions/ADR-[NNN]-[title].md`
- **명세서**: `docs/specs/SPEC-[feature-name].md`
- **진행 로그**: `docs/logs/YYYY-MM-DD-[description].md`

---

## [지침] 레퍼런스 및 외부 자료 관리 (강제)

사용자로부터 참조 파일이 필요한 상황이 발생하면, **반드시** 아래 절차를 따르세요.

### 레퍼런스 폴더 구조

```
references/
  01-design/          # 디자인 시안, 와이어프레임, UI 레퍼런스
  02-api-docs/        # API 문서, 스웨거, 스키마
  03-existing-code/   # 기존 코드, 라이브러리 참조
  04-data-samples/    # 샘플 데이터, 테스트 데이터
  05-style-guides/    # 문서 스타일 레퍼런스
  99-misc/            # 기타 참조 자료
```

### 수집 절차

1. 용도에 맞는 하위 폴더를 `references/` 아래에 생성
2. 사용자에게 구체적 경로를 안내하며 파일 배치 요청
3. 사용자가 파일을 넣었다고 알리면 해당 경로 확인
4. 레퍼런스를 읽고 분석한 후 작업에 반영

### 관리 규칙

- 레퍼런스 파일은 수정하지 않음 (읽기 전용 취급)
- 대용량 파일은 `.gitignore`에 추가 권장

---

## [지침] 문서 작성 품질 기준 (강제)

### 서식 규칙

- H1은 문서 제목 전용, 본문은 H2~H3만 사용합니다
- 불릿 목록: 하이픈(`-`) 통일 사용
- 코드 블록: 반드시 언어 지정 (```python, ```bash, ```json 등)
- 구분선(`---`): 대주제 전환 시에만 사용합니다

### 한국어 기술 문서 스타일

- 격식체(합쇼체)만 사용합니다: "~합니다", "~입니다"
- 영문 기술 용어 병기: "모듈화(Modularization)"
- 한 문장은 80자 이내 권장

---

## [작업 흐름] 매 작업 시 따라야 할 절차

### 작업 시작 시

1. PROGRESS.md에서 현재 진행 상태 확인
2. `git log --oneline -10`으로 최근 커밋 이력 확인
3. `git status`로 커밋되지 않은 변경사항 확인
4. 해당 작업을 TodoWrite에 등록
5. 작업 시작 전 관련 파일/모듈 읽기
6. 사용자에게 작업 시작 알림

### 작업 완료 시 (매 턴 종료 전 필수)

1. 해당 모듈의 README.md 업데이트
2. PROGRESS.md 업데이트 (완료 표시, 다음 작업 명시)
3. 사용자에게 완료 보고 및 다음 단계 확인
4. 사용자 승인 후 다음 작업 진행

### 에러 발생 시 복구 절차

- **코드 실수 발견 시**: 즉시 작업 중단 -> `git diff`로 변경 범위 확인 -> 문제 부분 복구
- **잘못된 방향으로 진행 시**: 체크포인트 커밋 생성 -> 대안을 사용자에게 제시
- **복구 불가능한 상황 시**: 최근 정상 커밋을 `git log`로 찾아 사용자에게 reset 제안 (사용자 확인 필수)
- **외부 API 오류 시**: 3회까지 재시도 -> 실패 시 우회 방법 탐색 -> 해결 불가 시 보고

---

## [보존] Compact Instructions

컨텍스트 압축(compaction) 시 반드시 보존해야 할 항목입니다.

- 현재 진행 중인 작업과 단계 (PROGRESS.md 참조)
- 수정된 파일 전체 목록
- 사용자가 내린 결정 사항과 승인 내역
- 다음 수행할 작업과 우선순위
- 프로젝트의 모듈 구조와 현재 작업 중인 모듈 경로

---

## [언어] 커뮤니케이션 및 인코딩 설정

### 언어 설정

- 사용자와의 대화: **한국어**
- 코드 내 변수/함수명: snake_case (Python 표준)
- 문서 작성: **한국어**
- 커밋 메시지: **한국어** (아래 형식 필수)

### 커밋 메시지 형식 (강제)

형식: `<타입>(<범위>): <요약>` 또는 `<타입>: <요약>`

| 타입 | 의미 | 예시 |
|------|------|------|
| 기능 | 새로운 기능 추가 | `기능(인증): 로그인 API 구현` |
| 수정 | 버그 수정 | `수정(파서): CSV 인코딩 오류 해결` |
| 문서 | 문서 변경 | `문서(전체): PROGRESS.md 업데이트` |
| 구조 | 리팩토링 | `구조(유틸): 공통 함수 모듈 분리` |
| 설정 | 설정/환경 변경 | `설정(빌드): 웹팩 최적화 추가` |
| 초기화 | 프로젝트 초기 세팅 | `초기화: 프로젝트 구조 세팅` |

### 이모지 사용 금지 (강제)

모든 영역에서 이모지 사용 금지. 상태 표시는 텍스트 마커 사용:
- 완료: `[v]` / 진행중: `[>]` / 대기: `[ ]` / 경고: `[!]` / 중요: `[*]`

### 한글 인코딩 규칙 (강제)

- 모든 파일은 **UTF-8 (BOM 없음)** 인코딩으로 읽기/쓰기
- 한글 경로 및 파일명은 반드시 큰따옴표로 감싸기
- 셸 명령 실행 전 필요 시 `chcp 65001` 로 코드페이지 설정
- Python 스크립트 실행 시 `PYTHONUTF8=1` 환경변수 고려

---

## [검증] 매 응답 전 자기 점검 (이 섹션은 반드시 CLAUDE.md 최하단에 위치)

응답을 완료하기 전에 다음을 확인합니다:
- 파일을 수정했다면, PROGRESS.md와 관련 README.md도 업데이트했는가?
- 작업이 완료되었다면, 사용자에게 다음 단계 승인을 요청했는가?
- 현재 작업 모듈 외의 파일을 수정하지 않았는가?
- 이모지를 사용하지 않고 텍스트 마커를 사용했는가?
- 새 파일 생성 시, 기존 파일 수정으로 불가능한지 먼저 확인했는가?
- 문서에 격식체(합쇼체)를 사용했는가?
- 한글 경로를 큰따옴표로 감쌌는가?
- 커밋 메시지가 `<타입>(<범위>): <요약>` 형식을 따르는가?
- 커밋되지 않은 변경사항이 남아있지 않은가?
